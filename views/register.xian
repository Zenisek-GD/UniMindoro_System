<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniMindoro - Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            overflow: hidden;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #0B1120 0%, #1A1F35 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            padding: 20px;
        }

        .background-blob {
            position: fixed;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.15;
            animation: float 20s infinite ease-in-out;
            z-index: 1;
        }

        .blob-1 {
            width: 400px;
            height: 400px;
            background: #00C6A9;
            top: -200px;
            left: -100px;
            animation-delay: 0s;
        }

        .blob-2 {
            width: 300px;
            height: 300px;
            background: #3CB4FF;
            bottom: -150px;
            right: -100px;
            animation-delay: -5s;
        }

        .blob-3 {
            width: 200px;
            height: 200px;
            background: #667eea;
            top: 20%;
            right: 10%;
            animation-delay: -10s;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            33% { transform: translateY(-20px) rotate(120deg); }
            66% { transform: translateY(20px) rotate(240deg); }
        }

        .register-container {
            background: rgba(21, 27, 43, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            box-shadow: 
                0 25px 50px -12px rgba(0, 0, 0, 0.5),
                0 0 0 1px rgba(255, 255, 255, 0.05);
            width: 100%;
            max-width: 420px;
            max-height: 90vh;
            position: relative;
            z-index: 10;
            overflow: hidden;
        }

        .register-content {
            padding: 40px 32px;
            max-height: 90vh;
            overflow-y: auto;
        }

        .register-content::-webkit-scrollbar {
            width: 4px;
        }

        .register-content::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 2px;
        }

        .register-content::-webkit-scrollbar-thumb {
            background: #00C6A9;
            border-radius: 2px;
        }

        .brand-text {
            font-weight: 700;
            background: linear-gradient(135deg, #00C6A9 0%, #3CB4FF 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .input-field {
            background: rgba(255, 255, 255, 0.05);
            border: 1.5px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            color: #EAEAEA;
            transition: all 0.3s ease;
            width: 100%;
        }

        .input-field::placeholder {
            color: #6B7280;
        }

        .input-field:focus {
            border-color: #00C6A9;
            box-shadow: 0 0 0 4px rgba(0, 198, 169, 0.1);
            background: rgba(255, 255, 255, 0.08);
            outline: none;
        }

        .register-btn {
            background: linear-gradient(135deg, #00C6A9 0%, #3CB4FF 100%);
            border-radius: 16px;
            color: white;
            font-weight: 600;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: none;
            cursor: pointer;
            width: 100%;
        }

        .register-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .register-btn:hover::before {
            left: 100%;
        }

        .register-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 25px rgba(0, 198, 169, 0.3);
        }

        .toggle-link {
            color: #00C6A9;
            transition: color 0.3s ease;
            text-decoration: none;
        }

        .toggle-link:hover {
            color: #3CB4FF;
        }

        .info-section {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .error-message {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
        }

        .checkbox {
            accent-color: #00C6A9;
        }

        @media (max-height: 700px) {
            .register-container {
                max-height: 95vh;
            }
            
            .register-content {
                padding: 30px 24px;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background Blobs -->
    <div class="background-blob blob-1"></div>
    <div class="background-blob blob-2"></div>
    <div class="background-blob blob-3"></div>

    <!-- Register Container -->
    <div class="register-container">
        <div class="register-content">
            <!-- Header -->
            <div class="text-center pb-6">
                <!-- Logo -->
                <div class="flex items-center justify-center space-x-3 mb-6">
                    <div class="w-14 h-14 bg-gradient-to-br from-[#00C6A9] to-[#3CB4FF] rounded-2xl flex items-center justify-center shadow-lg">
                        <i class="fas fa-user-plus text-white text-xl"></i>
                    </div>
                    <h1 class="text-4xl font-bold brand-text">UniMindoro</h1>
                </div>
                <p class="text-gray-400 text-sm font-medium">
                    Create Staff Account
                </p>
            </div>

            <!-- Registration Form -->
            <form action="/register" method="POST">
                {{#if error}}
                <div class="error-message rounded-xl p-4 mb-6">
                    <div class="flex items-center">
                        <i class="fas fa-exclamation-circle text-red-400 mr-3"></i>
                        <span class="text-red-300 text-sm font-medium">{{error}}</span>
                    </div>
                </div>
                {{/if}}

                <!-- Full Name -->
                <div class="mb-5">
                    <label class="block text-gray-300 text-sm font-semibold mb-3 uppercase tracking-wide text-xs" for="name">
                        Full Name
                    </label>
                    <div class="relative">
                        <input 
                            id="name" 
                            name="name" 
                            type="text" 
                            required 
                            class="input-field px-5 py-4 text-sm placeholder-gray-500 focus:outline-none focus:ring-0"
                            placeholder="John Doe"
                            value="{{name}}"
                        >
                        <div class="absolute right-4 top-1/2 transform -translate-y-1/2">
                            <i class="fas fa-user text-gray-500"></i>
                        </div>
                    </div>
                </div>

                <!-- Email -->
                <div class="mb-5">
                    <label class="block text-gray-300 text-sm font-semibold mb-3 uppercase tracking-wide text-xs" for="email">
                        Email Address
                    </label>
                    <div class="relative">
                        <input 
                            id="email" 
                            name="email" 
                            type="email" 
                            required 
                            class="input-field px-5 py-4 text-sm placeholder-gray-500 focus:outline-none focus:ring-0"
                            placeholder="staff@unimindoro.com"
                            value="{{email}}"
                        >
                        <div class="absolute right-4 top-1/2 transform -translate-y-1/2">
                            <i class="fas fa-envelope text-gray-500"></i>
                        </div>
                    </div>
                </div>

                <!-- Password -->
                <div class="mb-5">
                    <label class="block text-gray-300 text-sm font-semibold mb-3 uppercase tracking-wide text-xs" for="password">
                        Password
                    </label>
                    <div class="relative">
                        <input 
                            id="password" 
                            name="password" 
                            type="password" 
                            required 
                            class="input-field px-5 py-4 text-sm placeholder-gray-500 focus:outline-none focus:ring-0"
                            placeholder="••••••••"
                        >
                        <div class="absolute right-4 top-1/2 transform -translate-y-1/2">
                            <i class="fas fa-lock text-gray-500"></i>
                        </div>
                    </div>
                </div>

                <!-- Confirm Password -->
                <div class="mb-6">
                    <label class="block text-gray-300 text-sm font-semibold mb-3 uppercase tracking-wide text-xs" for="confirmPassword">
                        Confirm Password
                    </label>
                    <div class="relative">
                        <input 
                            id="confirmPassword" 
                            name="confirmPassword" 
                            type="password" 
                            required 
                            class="input-field px-5 py-4 text-sm placeholder-gray-500 focus:outline-none focus:ring-0"
                            placeholder="••••••••"
                        >
                        <div class="absolute right-4 top-1/2 transform -translate-y-1/2">
                            <i class="fas fa-lock text-gray-500"></i>
                        </div>
                    </div>
                </div>

                <!-- Hidden Role Field (Always staff) -->
                <input type="hidden" name="role" value="staff">

                <!-- Terms Agreement -->
                <div class="mb-6">
                    <label class="flex items-center cursor-pointer">
                        <input type="checkbox" name="agree-terms" required 
                            class="checkbox w-4 h-4 rounded border-gray-600 bg-gray-700 focus:ring-2 focus:ring-[#00C6A9]">
                        <span class="ml-3 text-sm text-gray-400">
                            I agree to the <a href="#" class="text-[#00C6A9] hover:text-[#3CB4FF] font-medium">Terms and Conditions</a>
                        </span>
                    </label>
                </div>

                <!-- Register Button -->
                <button type="submit" class="register-btn py-4 font-semibold text-sm mb-6 relative overflow-hidden">
                    <i class="fas fa-user-plus mr-2"></i>
                    CREATE STAFF ACCOUNT
                </button>
            </form>

            <!-- Info Section -->
            <div class="info-section rounded-2xl p-4 mb-6">
                <div class="flex items-start">
                    <i class="fas fa-info-circle text-[#00C6A9] mt-1 mr-3"></i>
                    <div class="text-sm text-gray-400">
                        <strong class="text-gray-300">Note:</strong> Admin accounts are created by system administrators. 
                        Contact your supervisor for admin access.
                    </div>
                </div>
            </div>

            <!-- Login Link -->
            <div class="border-t border-gray-800 pt-6 text-center">
                <p class="text-gray-400 text-sm">
                    Already have an account? 
                    <a href="/login" class="toggle-link font-semibold ml-1 transition-colors duration-200">
                        Sign in here
                    </a>
                </p>
            </div>
        </div>
    </div>

    <script>
        // Prevent form submission on enter key in inputs to avoid accidental submission
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Enter') {
                const activeElement = document.activeElement;
                if (activeElement.tagName === 'INPUT' && activeElement.type !== 'submit') {
                    e.preventDefault();
                }
            }
        });
    </script>
</body>
</html>